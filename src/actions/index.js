import {
    PROFILE_SET,
    PROFILE_REMOVE
} from './types';

export const setProfile = ( data ) => {
    console.log('hi',data);
    return dispatch => {
        /*dispatch({
            type: PROFILE_SET,
            payload: 
        })*/
    }
}
